

@{
  Layout = "_Layout";
}
@using HairSalon.Models

<header> 

@* <h1>Welcome To Eau Clair's Salon</h1> 
<h4>Add Client</h4> *@

<div>

<ul class="navbar"> 

  <li>@Html.ActionLink("Add Stylists", "Create", "Stylists", new {id = ViewBag.StylistId})</li>

</ul>
</header>
</div>

<div class="container">
  <h1>Welcome To Eau Clair's Salon</h1>

  <h2>Please select a Stylist</h2>

<hr />

@if (@Model.Count == 0)
{
    <h3>No Stylists have been added yet!</h3>
}

<ul>
    @foreach (Stylist stylist in Model)
    {
        <li>@Html.ActionLink($"{stylist.Name}", "Details", "Stylists", new { id = stylist.StylistId })</li>
    }
</ul>
</div>


   
  </div>
  @* <hr /> *@
  <div class="link-container">
   

    
  </div>